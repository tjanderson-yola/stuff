import java.util.*;

 public class Try {
  public static void main( String [] args )
  {
   Scanner myScan = new Scanner(System.in);
	//3 values are neede
	double a, b, c;
	a = myScan.nextDouble();
	b = myScan.nextDouble();
   c = myScan.nextDouble();
   System.out.println(Math.exp(a));
	System.out.println(Math.exp(b));
   System.out.println(Math.exp(c));

   }
 }

